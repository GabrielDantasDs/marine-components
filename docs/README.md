# Marine Components — Documentation

This directory contains the [Docusaurus](https://docusaurus.io/) documentation site for marine-components.

## Local development

```bash
cd docs
npm install
npm start
```

The site is automatically deployed to GitHub Pages on push to `main` via the workflow at `.github/workflows/deploy-docs.yml`.
